<%= simple_form_for([@project, @project_log]) do |f| %>    
 
  <%= f.input :subject, :label => "关于：" %>  
  <%= f.input :log, :label => "内容 ：", :input_html => {:rows => 4} %>   
  <%= f.button :submit, '保存' %>  
<% end %> 